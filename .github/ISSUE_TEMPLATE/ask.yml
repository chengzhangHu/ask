name: 问知识库
description: 基于公开已发布内容检索问答（匿名可问）；定时批量回复，约 1–2 小时，每日限流
title: "[Ask] "
labels: [ask-runtime]
body:
  - type: textarea
    id: question
    attributes:
      label: 你的问题
      description: 尽量具体，便于检索到最相关文章（≤500 字）
      placeholder: 例如：AI E2E 测试如何做质量门禁？
    validations:
      required: true
  - type: textarea
    id: context
    attributes:
      label: 背景（可选）
      description: 补充你想解决的场景或已有尝试
    validations:
      required: false